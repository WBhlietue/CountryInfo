use laravel;
create table Country
(
    officialName varchar(200) primary key,
    svg varchar(200),
    region varchar(200),
    subregion varchar(200),
    startOfWeek varchar(200),
    population varchar(200),
    area varchar(200),
    fifa varchar(200),
    capital varchar(200),
    timezones varchar(200),
    map varchar(200),
    cca2 varchar(200)
)

use laravel;
drop table Country

use laravel;
select * from Country

use laravel;
delete from Country